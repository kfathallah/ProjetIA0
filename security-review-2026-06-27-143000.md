# 🔒 Review de Sécurité - ProjetIA0

**Date** : 2026-06-27  
**Heure** : 14:30:00

---

## CRITIQUE 🔴

### 1. **Validation des entrées absente**
**Problème** : Pas de validation sur les paramètres d'entrée (valeurs négatives, null, débordements)

**Localisation** :
- `src/main/java/com/projetia0/model/Energie.java` - `quantiteKwh` peut être négatif
- `src/main/java/com/projetia0/model/Transport.java` - `distanceKm` peut être négatif, `mode` non validé
- `src/main/java/com/projetia0/model/Achat.java` - `montantEuros` peut être négatif
- `src/main/java/com/projetia0/model/Entreprise.java` - `nom` peut être null

**Impact** : Un attaquant pourrait injecter des valeurs malveillantes pour fausser les calculs.

**Exemple d'attaque** :
```java
new Energie("Électricité", -1000000, 0.058); // Valeur négative acceptée
```

### 2. **Pas de gestion explicite des erreurs**
**Problème** : `FacteursEmission.getTransportFactor()` masque les erreurs en retournant 0.0

**Localisation** : `src/main/java/com/projetia0/service/FacteursEmission.java` (ligne 14-15)

```java
return TRANSPORT_FACTEURS.getOrDefault(mode.toLowerCase(), 0.0); // Masque les erreurs
```

**Impact** : Un mode de transport invalide retourne silencieusement 0, produisant des rapports inexacts.

---

## ÉLEVÉ 🟠

### 3. **Pas d'immuabilité complète des objets de domaine**
**Problème** : Les objets retournés ne sont pas immutables et pas de protection des paramètres

**Localisation** : `src/main/java/com/projetia0/model/Entreprise.java` (ligne 31)

```java
public List<PosteEmission> getPostes() {
    return Collections.unmodifiableList(postes);
}
```

La liste est protégée, mais les objets primitifs n'ont pas d'accesseurs "setter" (bon), cependant les paramètres peuvent être modifiés en externe.

### 4. **Configuration en plain text**
**Problème** : `src/main/resources/facteurs-emission.properties` contient des données sensibles non chiffrées.

**Impact** : Modification non détectée des facteurs d'émission, risque d'injection de valeurs malveillantes.

### 5. **Pas de validation du nom de l'entreprise**
**Localisation** : `src/main/java/com/projetia0/model/Entreprise.java` (ligne 10-12)

```java
public Entreprise(String nom) {
    this.nom = nom; // Pas de null-check
}
```

**Impact** : `NullPointerException` possible lors du formatage du rapport.

---

## MOYEN 🟡

### 6. **Swing Thread-Safety incomplète**
**Localisation** : `src/main/java/com/projetia0/App.java` (ligne 15-17)

Bien que `SwingUtilities.invokeLater()` soit utilisé correctement, il n'y a pas de protection contre les modifications concurrentes de l'état UI.

### 7. **Pas de logging/audit**
Aucune journalisation des opérations critiques (ajout de poste, calcul d'empreinte). Impossible de tracer les modifications ou d'auditer les actions.

### 8. **Pas de limite sur la taille des données**
Pas de limite sur le nombre de postes qu'on peut ajouter (risque de DoS par consommation mémoire).

---

## BAS 🔵

### 9. **Version Maven instable (SNAPSHOT)**
**Localisation** : `pom.xml` (ligne 6)

Version `1.0-SNAPSHOT` utilisée - recommandé d'utiliser une version stable en production.

### 10. **Pas de vérification de dépendances vulnérables**
Pas de plugin de sécurité Maven pour vérifier les CVE dans les dépendances.

---

## 📋 Recommandations Prioritaires

| Priorité | Recommandation |
|----------|---|
| 🔴 CRITIQUE | Ajouter la validation des entrées sur tous les paramètres numériques (vérifier > 0) |
| 🔴 CRITIQUE | Implémenter une gestion explicite des erreurs et des exceptions |
| 🟠 ÉLEVÉ | Valider les entrées null et vides pour le nom de l'entreprise |
| 🟠 ÉLEVÉ | Protéger la configuration (chiffrement ou variables d'environnement) |
| 🟡 MOYEN | Ajouter du logging pour l'audit (SLF4J + Logback) |
| 🟡 MOYEN | Implémenter une limite sur le nombre de postes |
| 🔵 BAS | Ajouter le plugin `maven-dependency-check` pour détecter les CVE |
| 🔵 BAS | Utiliser une version stable au lieu de SNAPSHOT |

---

## Actions Recommandées

### Immédiat (Critique)
1. Ajouter des validations dans les constructeurs
2. Implémenter les vérifications de null
3. Ajouter des exceptions significatives

### Court terme (Élevé)
1. Intégrer un framework de logging (SLF4J)
2. Protéger les fichiers de configuration
3. Ajouter des tests de validation

### Moyen terme (Moyen)
1. Ajouter la vérification des dépendances Maven
2. Implémenter les limites de ressources
3. Améliorer les contrôles de concurrence

---

*Rapport généré automatiquement - Review effectuée sur l'ensemble du projet*
